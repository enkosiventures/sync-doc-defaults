export interface ExampleOptions {
  /**
   * Foo string.
   * @defaultValue {@link DEFAULTS.foo}
   */
  foo?: string;

  /**
   * Count number.
   * @defaultValue {@link DEFAULTS.count}
   */
  count?: number;

  /**
   * Enabled flag.
   * @defaultValue {@link DEFAULTS.enabled}
   */
  enabled?: boolean;
}
